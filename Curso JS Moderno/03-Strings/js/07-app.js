const producto = 'Monitor 20 pulgadas';

// "requerimos todos los texzxtos con mayus"
console.log(producto.toUpperCase());
console.log(producto.toLowerCase()); 

// Este sirve para cuando usamos correos electronicos y los usuarios hacen uso de mayusculas

const email = 'cOrReO@correo.COM';
console.log(email.toLowerCase());

const precio=300;
console.log(precio); // aparece como numero (de otro color)
console.log(precio.toString());
